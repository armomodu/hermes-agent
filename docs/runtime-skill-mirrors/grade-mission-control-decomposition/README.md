# Mission Control Decomposition Grader Runtime Skill Mirror

This directory mirrors the single active global runtime skill at:

- `~/.hermes/skills/grade-mission-control-decomposition/`

The mirror is the source-controlled authority for review and installation. It does not create a
second grader service or runtime instance. The grader selects semantic closure rules from the
objective delivery profile: software remains integration-proof/build strict, while artifact
delivery requires bounded content evidence, Maeve quality ownership, and the final operator gate.
